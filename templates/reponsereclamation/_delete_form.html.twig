<form method="post" action="{{ path('app_reponsereclamation_delete', {'idrep': reponsereclamation.idrep}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reponsereclamation.idrep) }}">
    <button class="btn">Delete</button>
</form>
