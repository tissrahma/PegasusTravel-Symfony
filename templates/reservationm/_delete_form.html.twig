<form method="post" action="{{ path('app_reservationm_delete', {'idRes': reservationm.idRes}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservationm.idRes) }}">
    <button class="btn">Delete</button>
</form>
