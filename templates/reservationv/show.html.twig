{% extends 'baseFront.html.twig' %}

{% block title %}Reservationv{% endblock %}

{% block body %}
    <h1>Votre Réservation de voyage</h1>

    <table class="table">
        <tbody>
          <!--  <tr>
                <th>Idr</th>
                <td>{{ reservationv.idr }}</td>
            </tr>-->
            <tr>
                <th>Nombre de personnes</th>
                <td>{{ reservationv.nbPersonnes }}</td>
            </tr>
            <tr>
                <th>Date du voyage</th>
                <td>{{ reservationv.date }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('app_reservationv_index') }}">Retour à la liste</a>

    <a class="btn btn-primary" href="{{ path('app_reservationv_edit', {'idr': reservationv.idr}) }}">Modifier</a>
    <a class="btn btn-primary" href="{{ path('imprimresv') }}">Imprimer votre réservation</a>
<br>

    {{ include('reservationv/_delete_form.html.twig') }}
    <br>

{% endblock %}
