<form method="post" action="{{ path('app_reclamation_delete', {'numero': reclamation.numero}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reclamation.numero) }}">
    <button class="btn"  style="background-color: white ;margin-bottom: 15px;width: 250px; ">Delete</button>
</form>
